import React from 'react'
import { ViewBase, View, Text, ScrollView } from 'react-native'

const Home = () => {
  return (
    <ScrollView>
      <Text>Home</Text>
    </ScrollView>
  )
}

export default Home
